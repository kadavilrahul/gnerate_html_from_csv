# Project Configuration
PROJECT_DIR=/var/www/yourproject  # Project directory (e.g., /var/www/yourproject)
BASE_DIR=/var/www/yourproject   # Base directory (usually the same as PROJECT_DIR)
BASE_URL=https://yourdomain.com  # Base URL of your website (e.g., https://yourdomain.com)

# Database Configuration
DB_HOST=your_db_host            # Database host (e.g., localhost or an IP address)
DB_USERNAME=products_user       # Database username
DB_PASSWORD=products_2@         # Database password
DB_NAME=products_db             # Database name

# MySQL Root Password (ONLY FOR INITIAL SETUP - REMOVE AFTER CREATING DATABASE AND USER)
MYSQL_ROOT_PASSWORD=your_mysql_root_password  # MySQL root password (for initial setup only!)

# API Configuration
API_URL=https://your-api.com/wp-json/wc/v3  # API URL (e.g., https://your-api.com/wp-json/wc/v3)
API_CONSUMER_KEY=your_api_consumer_key     # API consumer key
API_CONSUMER_SECRET=your_api_consumer_secret  # API consumer secret

# Cart URL Base
CART_URL_BASE=https://your-site.com/cart/  # Cart URL base (e.g., https://your-site.com/cart/)

# Navigation Links
NAV_SHOP=https://your-site.com/shop/           # Shop URL
NAV_CATEGORIES=https://your-site.com/categories/  # Categories URL
NAV_ABOUT=https://your-site.com/about/          # About URL
NAV_CONTACT=https://your-site.com/contact/        # Contact URL

# Logo Configuration
LOGO_URL=https://your-site.com/              # Logo URL
LOGO_IMAGE_URL=https://your-site.com/images/logo.png  # Logo image URL
LOGO_TITLE="Your Company Name"                # Logo title (Enclose in quotes if it contains spaces)